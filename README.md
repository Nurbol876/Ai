# 🎤 Nurbolai — ваш AI-интервьюер

**Nurbolai** — интерактивное веб-приложение для практики собеседований с использованием искусственного интеллекта. Приложение позволяет пользователям проходить реалистичные интервью, настраивать сложность, использовать голосовой ввод/вывод и сохранять историю сессий.

## ✨ Основные возможности

- **AI-интервьюер** — Интерактивное собеседование на основе ChatGPT с поддержкой кастомных вакансий
- **3 уровня сложности** — Легкий, средний и сложный режимы интервьюирования
- **Голосовой интерфейс** — Распознавание речи (Web Speech API) и синтез речи с настройкой (скорость, высота)
- **Сохранение сессий** — История разговоров в Firebase с возможностью продолжить или удалить
- **Аутентификация** — Вход через email/пароль или Google OAuth
- **Адаптивный дизайн** — Красивый glassmorphism UI, работает на всех устройствах

## 🛠 Стек технологий

- **Frontend**: React 19, Vite, CSS3
- **Backend**: Firebase (Authentication + Firestore)
- **AI**: OpenAI API (GPT-4o-mini)
- **Voice**: Web Speech API (распознавание + синтез речи)
- **Build**: Vite, ESLint

## 📦 Установка

### Предварительные требования
- Node.js 16+ и npm/yarn
- Firebase проект с включённой аутентификацией и Firestore
- OpenAI API ключ

### Шаги установки

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Nurbol876/Ai.git
   cd Ai
   ```

2. **Установите зависимости:**
   ```bash
   yarn install
   # или
   npm install
   ```

3. **Создайте файл `.env.local` с переменными окружения:**
   ```bash
   VITE_OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx
   VITE_FIREBASE_API_KEY=AIzaXxxxxxxxxxxxxxx
   VITE_FIREBASE_AUTH_DOMAIN=xxxxx.firebaseapp.com
   VITE_FIREBASE_PROJECT_ID=xxxxx
   VITE_FIREBASE_STORAGE_BUCKET=xxxxx.firebasestorage.app
   VITE_FIREBASE_MESSAGING_SENDER_ID=xxxxx
   VITE_FIREBASE_APP_ID=1:xxxxx:web:xxxxxxx
   ```

4. **Запустите dev-сервер:**
   ```bash
   yarn dev
   ```
   Приложение откроется на `http://localhost:5173`

## 🚀 Использование

### Для пользователя

1. **Создайте аккаунт** или войдите через Google
2. **Опишите вакансию** в текстовом поле (добавьте требования, стек, уровень опыта)
3. **Выберите стиль интервьюера** (Легкий, Средний, Сложный)
4. **Нажмите "Начать собеседование"** — AI начнёт задавать вопросы
5. **Отвечайте** текстом или голосом (нажмите кнопку 🎤)
6. **Сохраняйте сессии** для дальнейшей практики

### Просмотр сохранённых разговоров

На странице с описанием вакансии вы увидите список всех сохранённых разговоров с кнопками:
- **Продолжить** — загрузить разговор и продолжить диалог
- **Удалить** — удалить разговор из истории

## 📁 Структура проекта

```
src/
├── App.jsx              # Главный компонент с логикой интервью
├── App.css              # Стили приложения (glassmorphism)
├── main.jsx             # Точка входа React
├── index.css            # Глобальные стили
├── api.js               # Интеграция с OpenAI API
├── firebase.js          # Конфигурация Firebase
├── VoiceButton.jsx      # Компонент для голосового ввода
├── assets/              # Статические ресурсы
├── components/
│   └── AuthPanel.jsx    # Компонент аутентификации
└── context/
    └── AuthContext.jsx  # React Context для управления пользователем
```

## ⚙ Конфигурация

### Firebase

1. Создайте проект в [Firebase Console](https://console.firebase.google.com)
2. Включите **Authentication** (Email/Password + Google)
3. Создайте **Firestore Database** с правилами:
   ```javascript
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /users/{uid}/conversations/{document=**} {
         allow read, write: if request.auth.uid == uid;
       }
     }
   }
   ```
4. Скопируйте учётные данные в `.env.local`

### OpenAI API

1. Получите ключ на [platform.openai.com](https://platform.openai.com)
2. Добавьте в `.env.local` как `VITE_OPENAI_API_KEY`

## 📝 Скрипты

```bash
yarn dev      # Запустить dev-сервер
yarn build    # Собрать для production
yarn preview  # Предпросмотр build
yarn lint     # Проверка кода ESLint
```

## 🎨 Дизайн

Приложение использует современный **glassmorphism** дизайн с:
- Прозрачными панелями с blur-эффектом
- Голубыми и зелёными градиентами (glows)
- Мягкими переходами и hover-эффектами
- Полной адаптивностью для мобильных устройств

## 🔒 Безопасность

- Ключи API хранятся в переменных окружения (`.env.local`)
- Firebase Security Rules ограничивают доступ к личным данным
- OpenAI запросы делаются с сервера (если развернуть бэкенд)

## 🐛 Известные ограничения

- Голосовой ввод работает только в браузерах с поддержкой Web Speech API (Chrome, Edge, Safari)
- Синтез речи может отличаться в зависимости от браузера и ОС
- Максимум сохранённых разговоров ограничен лимитом Firestore



## 👤 Автор

**Nurbol876** — разработчик проекта Nurbolai

## 🤝 Контрибьютинг

Для отчётов об ошибках и предложений — откройте Issue на GitHub.

---

**Начните практиковаться сейчас!** 🚀

